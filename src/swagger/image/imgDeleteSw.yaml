paths:
  /image/delete:
    post:
      tags:
        - Image
      summary: delete image
      consumes:
        - application/json
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                img_url:
                  type: string
                  description: 이미지의 URL 값

      responses:
        200:
          description: "OK"
          content:
            application/json:
              schema:
                type: object
                properties:
                  item:
                    type: object
                    properties:
                  msg:
                    type: string
                    description: 메시지
                    example: 사진 삭제에 성공했습니다.
                  error:
                    type: boolean
                    description: error 체크
                    example: false

        500:
          description: "Server Error"

schemes:
  - https
  - http
